apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "techtestapp.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "techtestapp.name" . }}
    helm.sh/chart: {{ include "techtestapp.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "techtestapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
